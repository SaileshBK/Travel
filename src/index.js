import React from "react";
import  ReactDOM  from "react-dom";

import App from './App'

//mounting the app to the root div.
//Note : necessary for every single react app.
ReactDOM.render(<App />, document.getElementById('root'));
